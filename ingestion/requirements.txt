# Runtime dependencies for the orderbook ingestion pipeline.
# Installed in CI by .github/workflows/ingest.yml.

requests>=2.31,<3       # Cloudflare D1 HTTP API calls
python-dotenv>=1.0,<2   # load a local .env during development

# Added in later steps (kept out until the real logic lands):
# openai>=1.30          # field extraction
# firecrawl-py>=1.0     # page / PDF fetch + parse
