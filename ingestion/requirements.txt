# Runtime dependencies for the orderbook ingestion pipeline.
# Installed in CI by .github/workflows/ingest.yml.

requests>=2.31,<3       # D1 HTTP API + BSE / Scrape.do / Firecrawl REST calls
python-dotenv>=1.0,<2   # load a local .env during development

# Added in a later step (kept out until the real logic lands):
# openai>=1.30          # field extraction (Step 5)
